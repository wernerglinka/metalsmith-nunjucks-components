{% from "../partials/text.njk" import text %}
{% from "../partials/ctas.njk" import ctas %}

<section class="section-intro {% if params.paddingTop %}paddingTop{% endif %} {% if params.paddingBottom %}paddingBottom{% endif %}">
  <div class="content">
    {{ text(params.text) }}

    {% if params.hasCtas %}
      {{ ctas(params.ctas) }}
    {% endif %}
  </div>
</section>